# AI App Starter is BYOK (bring-your-own-key): the visitor supplies their API key
# in the browser at runtime. There is NO server-side API key and nothing secret
# is required to build, deploy, or run this app.
#
# Leave this file empty of secrets. If a project genuinely needs a non-secret
# build-time setting, document it here as KEY=value with a comment.
